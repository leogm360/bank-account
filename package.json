{
  "name": "bank-account",
  "version": "1.0.0",
  "description": "Bank account microservices monorepo.",
  "author": "Leonardo Moraes",
  "license": "MIT",
  "private": true,
  "packageManager": "pnpm@10.6.1",
  "engines": {
    "node": ">=v22"
  },
  "scripts": {
    "docker:dev:up": "sudo docker compose -f docker-compose.dev.yaml up -d",
    "docker:dev:down": "sudo docker compose -f docker-compose.dev.yaml down"
  }
}
